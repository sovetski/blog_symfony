{% extends 'base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}

    <!-- Page Content -->
    <div class="container">

        <!-- Page Heading/Breadcrumbs -->
        <h1 class="mt-4 mb-3">Blog Home One
            <small>Subheading</small>
        </h1>

        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="index.html">Home</a>
            </li>
            <li class="breadcrumb-item active">Blog Home 1</li>
        </ol>

        <div class="row">

            <!-- Blog Entries Column -->
            <div class="col-md-8">

                {% for article in articles %}

                <!-- Blog Post -->
                <div class="card mb-4">
                    <img class="card-img-top" src="{{ article.imageUrl }}">
                    <div class="card-body">
                        <h2 class="card-title">{{ article.title }}</h2>
                        <p class="card-text">
                            {{ article.content | slice(0, 200) }} ...
                        </p>
                        <a href="{{ path('app_article_show', {id: article.id}) }}" class="btn btn-primary">Lire plus &rarr;</a>
                    </div>
                    <div class="card-footer text-muted">
                        {{ article.publishedAt | date('d/m/Y à H:i:s') }}
                    </div>
                </div>

                {% endfor %}

                <!-- Pagination -->
                <ul class="pagination justify-content-center mb-4">
                    <li class="page-item">
                        <a class="page-link" href="#">&larr; Older</a>
                    </li>
                    <li class="page-item disabled">
                        <a class="page-link" href="#">Newer &rarr;</a>
                    </li>
                </ul>

            </div>

            <!-- Sidebar Widgets Column -->
            {% include('base.sidebar.html.twig') %}

        </div>
        <!-- /.row -->

    </div>
    <!-- /.container -->
{% endblock %}
