{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-lg-12 mt-5">
            {{
            include('form/general.html.twig', {
                form_var: registrationForm,
                action: 'Inscription',
                class: 'btn btn-primary'})
            }}
        </div>
    </div>
</div>
{% endblock %}
